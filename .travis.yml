language: generic 
sudo: required
services:
    - docker


before_install:
    - docker build -t wyorusso/docker-react -f Dockerfile.dev .


script:
    - docker run -e CI=true wyorusso/docker-react npm run test
    
